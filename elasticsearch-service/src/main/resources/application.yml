spring:
  elasticsearch:
      uris: http://localhost:9200

  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: elasticsearch-group
      auto-offset-reset: earliest

server:
  port: 8099
